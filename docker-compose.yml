version: '3.7'

services:
  web:
    build: .
    container_name: test_fast_api
    command: uvicorn app:my_api --port 5000 --host 0.0.0.0 --reload
    restart: always
#    env_file: .env
    ports:
      - "5000:5000"
#    volumes:
#      - ./app:/app/
#      - staticfiles:/app/static/
#    expose:
#      - 8000
#    env_file:
#      - ./.env
#    depends_on:
#      - db

#  db:
#    image: postgres
#    container_name: test_deploy_db
#    volumes:
#      - postgres_data:/var/lib/postgresql/data/
#    environment:
#      - POSTGRES_DB=postgres
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=hello


volumes:
  postgres_data:
  staticfiles:
